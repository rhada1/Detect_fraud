/**
 * config.js – Fill in your Render backend URL after deployment.
 * ⚠️  STUDENTS: replace the URL below with your own Render service URL.
 */
const CONFIG = {
    API_URL: "https://detect-fraud.onrender.com",   // ← change this
};
